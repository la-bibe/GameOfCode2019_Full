export class AllowedItems {
  head: number[];
  top: number[];
  bot: number[];

  constructor(head: number[], top: number[], bot: number[]) {
    this.head = head;
    this.top = top;
    this.bot = bot;
  }
}
