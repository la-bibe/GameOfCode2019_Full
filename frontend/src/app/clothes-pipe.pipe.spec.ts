import { ClothesPipePipe } from './clothes-pipe.pipe';

describe('ClothesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ClothesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
